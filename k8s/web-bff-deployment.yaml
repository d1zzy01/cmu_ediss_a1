apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-bff
  namespace: bookstore-ns
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-bff
  template:
    metadata:
      labels:
        app: web-bff
    spec:
      containers:
        - name: web-bff
          image: d1zzy0316/web-bff:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 9002
          env:
            - name: BOOK_SERVICE_URL
              value: "http://book-service:8001"
            - name: CUSTOMER_SERVICE_URL
              value: "http://customer-service:8002"
          livenessProbe:
            httpGet:
              path: /status
              port: 9002
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3